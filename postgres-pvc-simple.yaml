apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: postgres-pvc
  namespace: notes-app
  labels:
    app: postgres
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ebs-csi-gp3  # Default EKS storage class
  resources:
    requests:
      storage: 10Gi
